# CLI Inventory System

A lightweight, command-line Inventory Management System built in Go. Features modular business logic for products, customers, and orders, an interactive CLI interface, and built-in JSON file persistence.

## Features
- Modular architecture (`Product`, `Customer`, `Order`, and centralized `InventorySystem`)
- Interactive menu-driven CLI interface
- Local JSON data persistence (`inventory.json`)
